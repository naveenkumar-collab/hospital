import React from 'react';

// This route group is deprecated and its contents have been moved.
export default function DeprecatedPatientLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
